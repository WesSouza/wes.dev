---
import { Temporal } from 'temporal-polyfill';
import HeadMeta from '../../components/HeadMeta.astro';
import { MeetWindow } from '../../components/meet/MeetWindow';

console.log(Temporal.Now.zonedDateTimeISO().toString());

const freeTimeByWeekday = {
  1: {
    from: Temporal.PlainTime.from('08:00:00'),
    until: Temporal.PlainTime.from('22:59:59'),
  },
  2: {
    from: Temporal.PlainTime.from('08:00:00'),
    until: Temporal.PlainTime.from('22:59:59'),
  },
  3: {
    from: Temporal.PlainTime.from('08:00:00'),
    until: Temporal.PlainTime.from('22:59:59'),
  },
  4: {
    from: Temporal.PlainTime.from('08:00:00'),
    until: Temporal.PlainTime.from('22:59:59'),
  },
  5: {
    from: Temporal.PlainTime.from('08:00:00'),
    until: Temporal.PlainTime.from('22:59:59'),
  },
  6: {
    from: Temporal.PlainTime.from('08:00:00'),
    until: Temporal.PlainTime.from('22:59:59'),
  },
};
const maxDaysFromToday = 14;

const year = new Date().getFullYear();
---

<!DOCTYPE html>
<!-- Source code available at https://github.com/WesSouza/wes.dev/ -->
<html lang="en" class="-PurpleBackground">
  <head>
    <HeadMeta
      title="My Availability – Wes Souza"
      description="Check when I am free for a meeting."
    />
  </head>
  <body>
    <MeetWindow
      client:load
      freeTimeByWeekday={freeTimeByWeekday}
      maxDaysFromToday={maxDaysFromToday}
    />

    <footer class="Footer">
      <abbr title="Copyright">&copy;</abbr>
      {year} Wes Souza
    </footer>

    <style>
      html {
        height: 100%;
        color: var(--font-color);
      }

      body {
        display: flex;
        flex-direction: column;
        min-height: 100%;
        align-items: center;
        justify-content: center;
        margin: 0;
        padding: 50px;
        font-family: -apple-system, BlinkMacSystemFont, Roboto, sans-serif;
      }

      .Calendar {
      }

      .Column {
      }

      .Footer {
        border-top: 0.5px solid rgba(255, 255, 255, 0.1);
        margin-top: 50px;
        padding: 5px 10px;
        color: rgba(255, 255, 255, 0.65);
        font-size: 13rem;
      }
    </style>
  </body>
</html>
